# CLAUDE.md

This project's agent contract lives in [`AGENTS.md`](AGENTS.md). Read it before making any changes.

`AGENTS.md` is the single source of truth; `CLAUDE.md` only points to it. Keeping the rules in one place means they work across Claude Code, Cursor, Copilot, and anything else that reads either name.
